[pytest]
asyncio_mode = auto
testpaths = tests

# Add backend/ to sys.path so `import app` works when pytest is run from backend/.
# Requires pytest >= 7.0 (project uses 8.3.3).
pythonpath = .

python_files = test_*.py
python_classes = Test*
python_functions = test_*
